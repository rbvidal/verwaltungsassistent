package reasoning.ai.model;

/**
 * The role a finding plays in the hierarchy (primary, supporting, procedural, contextual, or collateral).
 */
public enum FindingRole {
    PRIMARY_FINDING,
    SUPPORTING_FINDING,
    PROCEDURAL_FINDING,
    CONTEXTUAL_FINDING,
    COLLATERAL_FINDING
}
